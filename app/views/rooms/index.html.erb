<p style="color: green"><%= notice %></p>

<h1>Rooms</h1>

<div id="rooms">
  <% @rooms.each do |room| %>
    <%= render room %>
      <p>
      <%= link_to "Show this room", houses_path %>
      </p>
  <% end %>
</div>
